# =============================================
# Environment Variables for Docker Deployment
# =============================================
# Copy this file to .env and fill in the values:
#   cp docker/.env.example .env
#   nano .env

# =============================================
# REQUIRED: Frontend Configuration
# =============================================
# Get these values from your Supabase / Lovable Cloud project
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# =============================================
# OPTIONAL: Port Configuration
# =============================================
FRONTEND_PORT=80
DB_PORT=5432

# =============================================
# OPTIONAL: Self-hosted Database Configuration
# =============================================
# Only needed if you're running the database container
POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=training_system

# =============================================
# REQUIRED: Cron Job Security
# =============================================
# Secret key for protecting reminder endpoints
# Generate with: openssl rand -hex 32
X_CRON_SECRET=your-cron-secret-key

# =============================================
# CRON JOB SETUP (crontab -e)
# =============================================
# Školení - každou hodinu v :00
# 0 * * * * curl -s -X POST "https://YOUR_SUPABASE_URL/functions/v1/send-training-reminders" -H "Content-Type: application/json" -H "x-cron-secret: YOUR_CRON_SECRET" >> /var/log/training-reminders.log 2>&1
#
# Technické události - každou hodinu v :05
# 5 * * * * curl -s -X POST "https://YOUR_SUPABASE_URL/functions/v1/run-deadline-reminders" -H "Content-Type: application/json" -H "x-cron-secret: YOUR_CRON_SECRET" >> /var/log/deadline-reminders.log 2>&1
#
# PLP prohlídky - každou hodinu v :10
# 10 * * * * curl -s -X POST "https://YOUR_SUPABASE_URL/functions/v1/run-medical-reminders" -H "Content-Type: application/json" -H "x-cron-secret: YOUR_CRON_SECRET" >> /var/log/medical-reminders.log 2>&1
