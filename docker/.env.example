# =============================================
# Environment Variables for Docker Deployment
# =============================================
# Copy this file to .env and fill in the values

# =============================================
# REQUIRED: Frontend Configuration
# =============================================
# Get these values from your Supabase project settings
# Dashboard -> Settings -> API

VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_PUBLISHABLE_KEY=your-anon-key
VITE_SUPABASE_PROJECT_ID=your-project-id

# =============================================
# OPTIONAL: Self-hosted Database Configuration
# =============================================
# Only needed if you're running your own PostgreSQL
# instead of using Supabase Cloud

POSTGRES_USER=postgres
POSTGRES_PASSWORD=your-secure-password
POSTGRES_DB=training_system

# =============================================
# OPTIONAL: SMTP Configuration
# =============================================
# Configure in the Admin UI instead of here
# The app reads SMTP settings from system_settings table

# =============================================
# OPTIONAL: Cron Job Configuration
# =============================================
# Secret key for protecting cron endpoints
# Generate with: openssl rand -hex 32

X_CRON_SECRET=your-cron-secret-key

# =============================================
# Example Cron Job (Linux/Unix)
# =============================================
# Add to crontab (crontab -e):
#
# # Run training reminders every Monday at 8:00 AM
# 0 8 * * 1 curl -X POST https://your-supabase-url/functions/v1/run-reminders \
#   -H "x-cron-secret: your-cron-secret-key" \
#   -H "Content-Type: application/json"
#
# # Run deadline reminders every day at 8:00 AM
# 0 8 * * * curl -X POST https://your-supabase-url/functions/v1/run-deadline-reminders \
#   -H "x-cron-secret: your-cron-secret-key" \
#   -H "Content-Type: application/json"
#
# # Run medical reminders every Monday at 9:00 AM
# 0 9 * * 1 curl -X POST https://your-supabase-url/functions/v1/run-medical-reminders \
#   -H "x-cron-secret: your-cron-secret-key" \
#   -H "Content-Type: application/json"
